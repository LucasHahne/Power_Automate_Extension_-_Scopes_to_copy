/**
 * UI Configuration Constants
 */
export const UI_CONFIG = {
  EXTENSION_WIDTH: 400,
  EXTENSION_MAX_HEIGHT: 500,
  COPY_FEEDBACK_DURATION: 1000, // milliseconds
} as const;

/**
 * Default gradient for list headers
 */
export const DEFAULT_GRADIENT = "from-blue-500 to-purple-600";
